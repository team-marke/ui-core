import Form from '../form';

/**
 * Default component for sending leads to HubSpot CRM
 * @param {HTMLFormElement} - The form element itself
 */
export default class HubspotForm extends Form {
  getFormData() {
    //TODO reescrever método
  }

  async submit() {
    //TODO reescrever método
  }
}
